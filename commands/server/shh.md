# Installieren
```
sudo apt update
sudo apt install openssh-server
sudo systemctl enable ssh
sudo ufw allow ssh
```
test
```
sudo systemctl status ssh
```
ip?
```
hostname -I
```
# starten
```
ssh <benutzername>@<ip>
```
